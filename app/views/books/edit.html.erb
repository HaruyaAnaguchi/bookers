<body>
<h1>Editing Book</h1>

<% if @book.errors.any? %>
<div class="error-box">
  <div class="error-title">
    <p class="error-title-message"><%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:</p>
  </div>
  <div class="error-body">
   <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
  </div>
</div>
<% end %>

<%= form_with model: @book,url:book_path, methods: :patch do |edit| %>

<div class="edet input">
<h4>Title</h4>
<%= edit.text_field :title %>
</div>

<div class="edit input">
<h4>Body</h4>
<%= edit.text_area :body %>
</div>

<div class="edit input">
<%= edit.submit 'Update Book' %>
</div>

<%= link_to 'show', book_path(@book.id) %>
|
<%= link_to 'back', books_path %>

<% end %>
</body>